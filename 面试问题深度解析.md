# 狼人杀项目面试问题深度解析

> 基于项目代码实际实现整理，每个问题包含：面试回答要点 + 代码出处 + 深入细节 + 常见追问。

---

## 整体结构

这份文档围绕 7 个核心面试问题展开，每个问题都遵循五层结构：**面试回答要点 → 代码出处 → 深入细节 → 常见追问 → 快速回忆卡片**。前四层是详细解释，最后一层是文末附录的速查表。下面按问题逐个用文字详细说明。

---

## 问题一：为什么用状态机模式设计游戏引擎

### 核心论点

狼人杀是强流程游戏，阶段链路是"等待 → 夜晚 → 死亡遗言 → 自由讨论 → 轮流发言 → 投票 → 循环 → 结束"，共 7 个阶段。每个阶段有独立的状态、定时器和合法操作集合，必须用状态机管理。

### 状态机的三个价值

1. **阶段切换原子化**：同一时刻只处于一个阶段，切换时清空旧定时器、广播新阶段，避免"夜晚还没结算就开始投票"这类竞态
2. **合法操作约束**：每个阶段只接受该阶段的操作，比如投票阶段提交夜晚行动会被直接忽略
3. **代码可维护性**：阶段切换有统一入口（`startXXX()` 方法），外部通过事件监听解耦

### 反面对比

如果不用状态机，会到处散落 `if (phase === 'night')` 判断，代码变成"意大利面条"，加新阶段（比如这次新增的 LAST_WILL/DISCUSSION）需要到处改条件分支。

### 代码出处

- 7 阶段枚举定义在 [constants.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/constants.js#L2-L10) 第 2-10 行
- GameEngine 继承 EventEmitter 在 [GameEngine.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/GameEngine.js#L1-L5) 第 1-5 行
- 阶段状态变量 `this.phase = PHASE.WAITING` 在第 15 行
- 夜晚总预算超时兜底 150 秒在 [GameEngine.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/GameEngine.js#L195-L216) 第 195-216 行

### 深入细节

完整的阶段流转链路（7 个状态）从 WAITING 开始，经过 startGame() 进入 NIGHT，然后 resolveNight 判胜负，如果未分胜负就 setTimeout 2 秒后 startDay。startDay 内部判断有无死者：有死者走 LAST_WILL 遗言阶段再进 DISCUSSION 自由讨论，无死者直接进 DISCUSSION。DISCUSSION 45 秒后进 DAY 轮流发言，发言完进 VOTE 投票，平票进 PK 加赛（一次，再平票无人出局），分胜负后要么 END 结束，要么 setTimeout 3 秒回到 NIGHT 循环。

每次阶段切换统一做三件事：`clearTimer()` 清旧定时器、`this.phase = PHASE.XXX` 原子更新状态、`broadcast('phase_change', ...)` 通知前端和外部监听器。

### 常见追问

**问：加一个新阶段要改多少代码？**

答：以加"警长竞选阶段"为例，只需在 constants.js 加枚举、GameEngine 加 startSheriffElection() 方法、相关 transition 处调用、Socket 事件处理器加对应事件、外部模块的 switch(phase) 加新 case。整体改动很小，状态机把变化收敛在几个固定位置。

---

## 问题二：状态机怎么实现的

### 核心论点

用 Node.js 原生 EventEmitter 自己实现，没有引入任何第三方状态机库。原因是 7 个阶段不算多，自己写可以精确控制每个阶段的定时器、状态变量和外部解耦。

### 核心结构四点

1. **阶段切换**：startXXX() 系列方法（startNight、startLastWill、startFreeDiscussion、startOrderedSpeaking、startVote、endGame），每个方法原子化地更新 phase + 清理定时器 + 广播 phase_change 事件
2. **外部通信**：构造函数注入 emit 回调，让 GameEngine 不直接依赖 Socket.IO，方便测试和移植
3. **外部监听解耦**：gameHandler 监听 emit 回调做持久化（处理 `__game_result`、`__game_replay` 特殊事件），AIGameHandler 监听 phase_change 触发 AI 阶段动作
4. **夜晚串行流程**：守卫 → 狼人 → 预言家 → 女巫 严格顺序，`_waitForRoleActions` 用 Promise + 事件监听等待每个角色的行动提交

### 代码出处

- 继承 EventEmitter + 注入 emit 回调在 [GameEngine.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/GameEngine.js#L5-L12) 第 5-12 行
- 夜晚严格角色顺序在第 218-370 行
- AIGameHandler 监听阶段切换在 [AIGameHandler.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/AIGameHandler.js#L81-L99) 第 81-99 行

### 深入细节

**为什么用 EventEmitter**：GameEngine 继承 EventEmitter 可以用 emit/on 做内部事件，比如 `_waitForRoleActions` 监听一个 `night_action_submitted` 事件，玩家提交行动时触发，结合超时检查，优雅地等所有狼人都投完杀人目标。

**夜晚严格串行顺序的必要性**：狼人杀有因果依赖——必须先知道守卫守了谁、狼人杀了谁，女巫的行动才有依据（要不要救人？毒谁？），所以女巫不能和狼人并行。代码写死顺序：1. 守卫守护（30s）2. 狼人杀人（30s）3. 预言家查验（30s）4. 女巫行动（30s），全部完毕后 resolveNight。为了防止夜晚卡壳，runNightSequence 外层有 150 秒总预算（Promise.race 赛跑），超时后强制 resolveNight。

**emit 回调的设计巧思**：GameEngine 不直接 `require('../app').getIO()` 依赖 Socket.IO，而是构造时传入 emit(roomCode, event, data) 回调。gameHandler 传入一个中间层 emit，拦截特殊事件：`__game_result` 做数据库持久化、`__game_replay` 生成复盘消息、`phase_change` 同时触发 AIGameHandler。这样 GameEngine 是纯粹的"业务引擎"，可以单独实例化单元测试，不需要 Socket.IO 环境。

### 常见追问

**问：为什么不用 XState 这类成熟库？**

答：试过但决定自己写。三个原因：① 只有 7 个阶段，手写不超过 300 行，库的 overhead 不划算；② 每个阶段的定时器和合法操作差异巨大，库的通用配置反而比手写代码长；③ 需要在状态切换里嵌入复杂的夜晚串行逻辑、PK 加赛、猎人开枪，自己写 startXXX() 方法更灵活。

---

## 问题三：并发问题怎么处理

### 核心论点

单实例 per room + 单线程天然串行 + 统一收集后集中结算。

### 三层保障

1. **Node.js 单线程**：JavaScript 是单线程事件循环，同一个 room 的 GameEngine 实例上所有 submit 方法调用都是串行的，不会有两个线程同时写 nightActions 的竞态
2. **房间级隔离**：gameCache 里每个 roomCode 对应独立的 GameEngine 实例，房间之间完全隔离
3. **先收集后结算**：夜晚行动都写入 nightActions 对象，玩家提交时不立即结算死亡结果，所有角色行动完毕后 resolveNight 统一结算；投票同理，votes 对象先收集，resolveVote 统一计票

### 代码出处

- nightActions 收集对象在 [GameEngine.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/GameEngine.js#L20) 第 20 行
- submitNightAction 写入校验 + 收集在第 443-481 行
- resolveNight 统一结算在第 520 行起
- votes 收集对象在第 29 行
- submitVote 在第 1357-1382 行
- resolveVote 统一计票 + PK 加赛在第 1384-1420 行
- 多狼投票多数决在第 287-310 行

### 深入细节

**submitNightAction 的防御性校验**：提交也不是随便写。代码里做了四道校验——女巫解药已用就 return、毒药已用就 return、守卫不能连守同一人就 return、狼人不能刀队友就 return。即使并发提交，每个 action 经过校验后再写入，同一个 socketId 第二次提交会被覆盖（非女巫），女巫允许保存两次（解药和毒药）。

**多狼投票多数决**：所有狼人各投各的，存入 nightActions。狼人阶段结束后统一计票：过滤出 action === 'kill' 的狼人行动，遍历统计每个 targetId 的票数，取票数最高的 target，平票时取第一个 topTarget（逻辑上应该随机，这是可优化的点）。

**横向扩展怎么办**：目前单进程架构，GameEngine 在内存里。横向扩展需要把状态外置 Redis + 用 Redis 锁锁房间操作（比如 SETNX lock:room:ABC123 拿锁，操作完 DEL 释放）。不过目前单进程 Node 扛几百个房间同时玩没问题，暂时没做。

### 常见追问

**问：两个真人和一个狼人同时提交 kill 最后听谁的？**

答：见多数决逻辑。不会"谁先到谁先生效"，所有狼人提交的都是独立 vote，最后统一 tally。如果两个狼人各投不同目标 1:1，取第一个 topTarget（可以主动说这是可优化点，面试加分）。

**问：submitNightAction 是同步还是异步，会不会 submit 完还没写入 resolve 就读空？**

答：submit 是纯同步写对象的操作，resolve 是在 `_waitForRoleActions` resolve 之后、所有行动都写完了才调用，时序严格保证不会读空。

---

## 问题四：断线重连怎么实现

### 核心论点

标记式断线 + 宽限期超时 + Redis 备份恢复，而不是简单地把玩家删除。

### 三层实现

1. **Socket 映射层**：userSocketMap.js 维护 userId → socketId 的 Map，断线时不清空 player 对象，只把 socketId = null + disconnectTime = Date.now()，这样 userId 和玩家身份（座位、角色、存活状态）都保留了
2. **前端自动恢复**：LoginView.vue 登录成功后检查 localStorage.savedRoomCode，如果有就自动 joinRoom；Socket 重连时自动用保存的 token 认证
3. **超时清理**：游戏进行中玩家断线 60s 宽限期未重连才真正删除；等待室 30s 超时删除；房主离开自动转让房主

### 代码出处

- userSocketMap 的 Map 结构 + 清理逻辑在 [userSocketMap.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/utils/userSocketMap.js#L1-L72) 第 1-72 行
- 标记式断线（socketId=null + disconnectTime）在第 43-60 行
- 60s/30s 超时清理逻辑在 roomHandler.js handleDisconnect 内部
- 前端 localStorage.savedRoomCode 自动重连在 stores/room.js joinRoom
- gameCache + Redis 快照备份在 utils/cache.js

### 深入细节

**清理旧连接时为什么不直接删 player**：正确做法是 player.socketId = null + disconnectTime = Date.now()，保留 userId、座位号、角色、isAlive，等新 socketId 来直接填回去。错误做法是直接 filter 删除——玩家身份、准备状态、角色都丢了，重连也无法恢复游戏。

**重连时的角色映射同步**：断线时 game.roles[oldSocketId] 的角色映射也删了（因为 socketId 变了），需要恢复。实际实现里，role 是持久化在 player.role 字段上的（RoomView 里看不到角色，但后端 player 对象上有 role 备份），重连时通过 userId 匹配玩家后，把新 socketId 写入角色映射：`game.roles[newSocketId] = existingPlayer.role`，再 delete game.roles[null]。

**Redis 兜底**：如果游戏进程崩溃了（Node 进程挂了），内存里的 GameEngine 全丢。cache.js 实现了写入 roomCache 的同时异步写入 Redis（werewolf:games:*），进程重启后可以尝试从 Redis 恢复快照（当前是降级策略，不会阻塞游戏启动）。

### 常见追问

**问：多设备登录时旧连接还在发消息怎么办？**

答：kickOldSocket 时做了三件事防野操作：① `oldSocket.kicked = true` 标记，所有 socket 事件处理器第一行就检查 `if (socket.kicked) return`；② 清理旧 socket 在房间里的状态（cleanUpOldSocketRoom）；③ 500ms 延迟后强制 oldSocket.disconnect(true)。

**问：玩家在白天发言阶段断线了轮到他发言怎么办？**

答：`_triggerAISpeaking` 有 5 秒 LLM 超时。真人玩家断线时，发言超时自动 nextSpeaker() 跳到下一个。回来时如果还在白天阶段，可以通过前端的 speaker_change 事件看到轮到谁了。

---

## 问题五：AI 用了什么模型怎么调用

### 核心论点

模型是小米 mimo-v2-flash（OpenAI 兼容 API 格式），也支持通过环境变量切 DeepSeek 等任何 OpenAI 兼容模型。

### 选型原因（按重要度）

1. **OpenAI 兼容接口**：用 LangChain 的 ChatOpenAI 封装，换模型只改 baseURL + apiKey，不用改代码
2. **成本低**：mimo-v2-flash 价格便宜，大量并发 LLM 调用（每个 AI 每晚 1 次决策 + 1-2 次发言 + 1 次投票）成本可控
3. **国内访问稳定**：相比 OpenAI 原生（需要梯子），国内 API 延迟低且稳定
4. **用户可自配置**：每个用户可以在个人中心页面配置自己的 API Key / URL / Model，不用统一绑定平台

### 调用链路四步

1. LangChain 封装：ChatOpenAI 创建模型实例，默认 temperature=0.7、maxTokens=500
2. Prompt 构造：PromptTemplate 拼接角色、阵营、存活玩家、队友、可疑玩家、游戏历史、人格等上下文
3. 结构化输出约束：StructuredOutputParser.fromNamesAndDescriptions 强制 LLM 输出 JSON 格式（比如夜晚行动输出 `{ action, targetId, reasoning }`）
4. 输出解析：`prompt.pipe(model).pipe(outputParser)` 串联成 Chain，invoke 后直接拿到结构化对象

### 代码出处

- 模型初始化（多环境变量 fallback）在 [AIGameHandler.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/AIGameHandler.js#L37-L56) 第 37-56 行
- AI 玩家创建（agentConfig 注入）在第 58-79 行
- 夜晚决策 + StructuredOutputParser 在第 123-204 行
- 投票决策 Chain 在第 296-369 行
- 发言生成 Prompt 在第 448-695 行
- 人格动态 temperature 在第 966-977 行
- pLimit 并发控制在第 10-26 行

### 深入细节

**为什么用 StructuredOutputParser**：如果不强制结构化输出，LLM 输出"我觉得应该杀 3 号"这种自然语言，后续解析还得靠正则，容易出错。用 Parser 可以让 LLM 严格输出 JSON，比如 `{"action": "kill", "targetId": "socket_id_of_player_3", "reasoning": "3号发言很可疑..."}`。LangChain 的 Parser 会在 Prompt 里自动注入格式说明（JSON Markdown 示例 + 字段描述），LLM 遵循率很高。

**并发控制 pLimit(5) 的必要性**：一局 12 人有 8 个 AI，晚上同时调用 LLM 会导致 ① API 限流被封（多数服务商有 RPM 限制）② 8 路 500+ token 的请求打过来瞬间响应太慢。自己写了 pLimit（每次最多 5 路 active，多的排队），8 个 AI 是前 5 个并行、后 3 个等出队。

**人格动态 temperature**：默认 temperature=0.7，但不同 AI 不应该一样发散/收敛。代码里根据人格五维调整：谨慎度 >70 就 temp -= 0.2（更保守），谨慎度 <30 就 temp += 0.15（更发散），话痨 >70 就 temp += 0.1（更发散），最后 clamp 到 0.3-1.0 区间。每次生成发言前调整 model.temperature，发言完不用重置（下次还会重设）。

### 常见追问

**问：StructuredOutputParser 解析失败怎么办？**

答：在 try/catch 里捕获，失败就调用对应的 Fallback 方法（`_getFallbackNightAction` / `_getFallbackVote` / `_getFallbackChatMessage`），保证游戏不卡。

**问：同一个模型不同 AI 的发言风格怎么区分？**

答：靠 Prompt 工程，不靠模型实例。每次调用时注入这个 AI 专属的 personality 描述（激进度/狡猾度翻译成自然语言）、speakingStyle 描述（幽默/严肃/咄咄逼人/冷静/神秘）、language 习惯（口头禅 prefixes/suffixes/favoriteWords）、策略配置（nightAction/dayStrategy/revealIdentity）。同样的模型，Prompt 里的 persona 不一样，输出就天差地别。

---

## 问题六：AI 如果 LLM 挂了怎么办

### 核心论点

三层 Fallback 机制，保证任何情况下 AI 都能出结果，绝不卡死游戏。

### 三层

1. **第一层：LLM 调用异常 → Fallback 模板**：try/catch 包裹每个 LLM 调用，catch 时调用对应的 `_getFallbackXXX` 方法（夜晚/投票/发言都有独立 fallback）
2. **第二层：Fallback 为空 → 紧急兜底**：如果 fallback 也返回空（比如模板数组为空、XX 替换失败），再给一个硬编码的兜底消息
3. **第三层：超时 → 自动 Fallback**：每个 AI 决策都加了 Promise.race 超时，夜晚 3s / 投票 3s / 发言 5s 没返回就不等了，直接走 fallback

另外，`_initModel` 时如果没配置 API Key 就直接把 `this.model = null`，后续每个 `_decideXXX` 方法第一行就 `if (!this.model) return fallback`，根本不进 LLM 调用路径。

### 代码出处

- model 为空直接走 fallback 在 [AIGameHandler.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/AIGameHandler.js#L124-L126) 第 124-126 行、297-299 行、448-451 行
- 夜晚 Promise.race(LLM, 3000ms) 在 [GameEngine.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/GameEngine.js#L407-L436) 第 407-436 行
- 投票 Promise.race(LLM, 3000ms) 在第 1326-1355 行
- 发言 Promise.race(LLM, 5000ms) 在第 1063-1092 行
- 夜晚 fallback 模板在 [AIGameHandler.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/game/AIGameHandler.js#L206-L227) 第 206-227 行
- 投票 fallback 模板在第 371-390 行
- 发言 fallback 模板（每角色 10+ 模板 × 随机 XX 替换）在第 650-742 行

### 深入细节

**夜晚 Fallback 示例**：代码里 `_getFallbackNightAction` 从存活其他玩家中随机选一个 target，根据角色返回不同 action——狼人返回 kill、预言家返回 check、守卫返回 guard、女巫如果解药没用且有人被狼杀就返回 save 否则返回 skip。女巫 fallback 还做了基本策略判断（能救就救），不是完全随机。

**投票 Fallback 策略**：狼人阵营过滤掉狼人队友，从好人里随机投；好人阵营也是随机投。注释里写了"fallback 是作弊版"（God's Eye，game 里所有角色信息都能拿到）。

**发言 Fallback**：角色 × 10+ 模板 × 随机玩家名替换。每个角色有 10+ 条预设模板发言，里面有 XX 占位符，替换为随机存活玩家名。这样即使 LLM 完全不可用，AI 也能发出"不像 AI 的"正常发言。

### 常见追问

**问：Fallback 里好人投票能看到狼人身份是不是作弊？**

答：是的，Fallback 投票版本是 God's Eye（game 里所有角色信息都能拿到）。如果要更真实，fallback 里应该改成纯随机投票（不知道谁是狼）。但实际 LLM 调用率很高（90%+），fallback 只是应急。

**问：如果 LLM 服务挂了 30 分钟整局游戏体验会怎样？**

答：体验会下降（AI 发言变模板化、策略变随机），但游戏流程完全不受影响。每个阶段有超时自动 fallback，不会卡。fallback 聊天的模板量够大（6 角色 × 10+ 模板），短时间打几局不会让玩家感觉重复。

---

## 问题七：Socket.IO 怎么做认证

### 核心论点

是同一套 JWT，双端共用一个密钥。区别在于传输方式。

### 两种传输方式

- **REST 认证**：HTTP 请求头 `Authorization: Bearer <token>`，authMiddleware 解析，401 时前端自动跳登录
- **Socket.IO 认证**：连接时从 `socket.handshake.auth.token` 或 `socket.handshake.headers.authorization` 取 token，`io.use(authMiddleware)` 里验证，失败返回 `AUTH_REQUIRED` / `AUTH_FAILED` 错误

### 认证通过后统一处理四步

1. `jwt.verify` 解码拿到 decoded.id / decoded.username
2. 挂载到 socket 对象上：`socket.userId = decoded.id`、`socket.username = decoded.username`
3. 写入 socketCache 供后续事件处理器用
4. 触发 kickOldSocket 多设备踢出旧连接

### 代码出处

- Socket.IO 认证中间件在 [socket/index.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/socket/index.js#L10-L44) 第 10-44 行
- REST authMiddleware 在 [auth.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/middleware/auth.js#L1-L18) 第 1-18 行
- 多设备踢出在 [userSocketMap.js](file:///d:/bandf/前后端/Newwerewolf/backend/src/utils/userSocketMap.js#L74-L100) 第 74-100 行
- 前端 socket.js authenticate + 错误处理
- JWT 签发在 routes/auth.js POST /login
- 接口限流（认证 20/分钟、通用 100/分钟）在 app.js

### 深入细节

**Socket.IO 中间件为什么用 io.use 而不是 socket.on('connection') 里面验证**：`io.use` 是"连接前"执行的中间件，验证不通过直接不让连接，攻击者拿不到 socket 对象。如果写在 connection 事件里再验证，攻击方已经建立了连接，可以滥发事件打服务。正确做法是在 io.use 里 `next(new Error('AUTH_REQUIRED'))` 不让连接；错误做法是 connection 进来再 disconnect，已经建立连接了。

**同一账号多设备踢出流程**：kickOldSocket 函数做了五件事：① cleanUpOldSocketRoom 清理房间状态；② oldSocket.emit('force_logout') 通知前端弹提示（reason: 'ACCOUNT_USED_ANOTHER_DEVICE'，message: '您的账号已在其他设备登录'）；③ `oldSocket.kicked = true` 标记防野操作；④ setTimeout 500ms 后 oldSocket.disconnect(true)；⑤ userToSocket.set(userId, newSocketId) 更新映射。500ms 延迟断开是个工程细节——force_logout 消息发出去后，需要给前端几秒处理弹窗和跳转，直接 disconnect 前端就收不到了。

**前端的双端联动**：Axios 响应拦截器：401 → 清除 token → 跳 /login；Socket `AUTH_REQUIRED`/`AUTH_FAILED`/`force_logout` 事件 → 清除 token → 跳 /login?forceLogout=1；LoginView.vue 里读 URL 参数，显示"您的账号已在其他设备登录"的 toast。

### 常见追问

**问：JWT 过期了怎么办，refresh token 做了吗？**

答：目前没做 refresh token，JWT 过期了直接让用户重新登录。JWT 有效期默认 7 天（JWT_EXPIRES_IN 可配置），对社交游戏场景来说够用。做 refresh token 可以改造：数据库加 refresh_tokens 表，REST /refresh 接口签发新 token，Socket 层面加 refresh 事件。如果面试官追问可以说"目前没做，如果要上线正式产品我会加这一层"。

**问：Socket 里的 JWT 和 HTTP 里的 JWT 是同一个吗，会不会不一致？**

答：是同一个 token，前端登录成功后存在 localStorage.getItem('token') 里，Axios 拦截器和 socket.connect 都从同一个地方读。只要用户不手动篡改，始终是同一个 token。

---

## 整体使用建议

1. **面试前 1 小时**：通读全部 7 个问题的"面试回答要点"段落，这是脱稿讲的话术
2. **面试前 10 分钟**：扫文末"快速回忆卡片"，记核心数字（7 阶段、150s、pLimit(5)、3s/5s 超时、500ms 延迟）
3. **被问到具体问题时**：先讲要点，再展开"深入细节"里的工程巧思（比如 emit 回调解耦、标记式断线、io.use 前置中间件）
4. **被追问时**：回忆"常见追问"段落，特别是那些主动暴露优化空间的回答（比如平票取第一个 topTarget 可优化、refresh token 没做但知道怎么做），体现思辨能力

---

## 附录：快速回忆卡片

| 问题 | 一句话核心答案 | 数字/细节 |
|------|---------------|-----------|
| Q1 状态机为什么 | 强流程游戏，原子切换防竞态 | 7 个阶段 |
| Q2 状态机怎么实现 | EventEmitter 手写，startXXX() 统一入口 | 夜晚严格串行，150s 总兜底 |
| Q3 并发怎么处理 | 单线程 + 单实例/room + 先收集后结算 | 夜晚 30s/角色，多狼多数决 |
| Q4 断线重连 | socketId=null 标记 + 60s 游戏宽限 | gameCache + Redis 快照 |
| Q5 AI 模型 | mimo-v2-flash，LangChain + StructuredOutputParser | pLimit(5) 并发，temperature 0.3-1.0 动态 |
| Q6 LLM 挂了 | 三层 Fallback：catch → empty → timeout | 夜晚 3s / 投票 3s / 发言 5s 超时 |
| Q7 Socket 认证 | 与 REST 同 JWT，io.use 中间件前置挡 | 500ms 延迟断开旧连接 |

这张表可以在面试前 5 分钟扫一眼，把核心数字和关键词印在脑子里，被问到时第一时间抛出来。
