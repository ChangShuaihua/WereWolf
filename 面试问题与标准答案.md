# Werewolf-AI 面试问题与标准答案

本文档整理了狼人杀 AI 项目在面试中最常被追问的问题，并给出适合项目答辩的标准回答。回答可以直接背诵，实际面试时再结合你的真实实现细节微调。

## 一、项目整体

### 1. 你这个项目的整体架构是怎样的？
**标准答案：**  
项目整体采用前后端分离架构。前端使用 Vue3 + Pinia 负责页面展示、状态管理和交互；后端使用 Node.js + Express 提供 HTTP 接口，Socket.IO 负责实时通信；MySQL 用于持久化用户、房间、对局记录等数据；JWT 用于登录鉴权；LangChain 配合 OpenAI 兼容 API 驱动 AI 玩家决策。实时游戏状态主要在后端内存中维护，关键结果和历史记录落库。

### 2. 前端、后端、AI 模块分别负责什么？
**标准答案：**  
前端负责展示房间、游戏阶段、玩家状态和交互按钮，同时通过 Socket.IO 接收实时事件。后端负责用户认证、房间管理、游戏状态校验、角色技能结算和胜负判定。AI 模块负责把游戏状态、角色身份、历史发言和人格参数组织成上下文，让 AI 完成发言、投票和夜间行动。

### 3. 为什么选择 Node.js + Express，而不是 Java 或 Spring Boot？
**标准答案：**  
这个项目的核心是实时通信和事件驱动流程，Node.js 在处理 Socket.IO、事件回调和 I/O 密集型任务时比较自然，开发效率也高。Express 结构轻量，适合快速搭建 API 和和 Socket 服务协同。对这个项目来说，重点不是复杂事务系统，而是高频状态同步和游戏流程编排，所以 Node.js 更合适。

### 4. 为什么使用 Socket.IO，而不是普通 HTTP 轮询？
**标准答案：**  
狼人杀是强实时游戏，玩家发言、投票、夜间行动和阶段切换都需要低延迟同步。HTTP 轮询会带来额外开销，而且时效性差。Socket.IO 支持长连接、广播、房间机制和断线重连，特别适合这种多人房间内的实时状态同步。

### 5. 这个项目里你觉得最难的部分是什么？
**标准答案：**  
最难的部分是把狼人杀规则、多人同步和 AI 行为统一到同一套状态体系里。因为狼人杀既有公开信息，也有角色私密信息，还存在阶段限制和行动顺序限制。要同时保证规则正确、事件一致、AI 可控、断线可恢复，这比普通聊天室复杂很多。

### 6. 你在项目中主要负责哪些模块？
**标准答案：**  
我主要负责游戏引擎、Socket 实时通信、AI Agent 接入和 AI 人格系统。具体包括状态机设计、角色技能结算、阶段同步、断线重连、多设备登录控制、Prompt 组织、AI 行为约束，以及游戏记录和复盘数据的持久化。

### 7. 如果让你重新设计一次，有哪些地方会优化？
**标准答案：**  
我会把游戏状态快照和事件日志再拆得更清晰一点，方便后续回放和恢复。其次会把 AI 的输出协议进一步标准化，比如统一为结构化 JSON，并把校验和重试机制做得更完整。另外，如果要支持更高并发，我会把房间状态从单机内存扩展到 Redis 或消息队列方案。

### 8. 这个项目和普通聊天室项目相比，复杂点在哪里？
**标准答案：**  
普通聊天室只需要处理消息收发和在线状态，而狼人杀要同时处理阶段流转、角色技能、投票结算、胜负判定、隐藏信息和 AI 决策。也就是说，它不仅是通信问题，更是规则引擎、状态机和多方协同的问题。

### 9. AI 玩家和真人玩家在系统中是如何统一建模的？
**标准答案：**  
我会把 AI 玩家和真人玩家都抽象成统一的玩家实体，字段上区分 `type`、`isAI`、`role`、`alive`、`seatIndex`、`socketId` 等信息。对游戏引擎来说，判断的都是“玩家”这个统一对象，只是在决策来源上，真人来自前端操作，AI 来自后端 Agent。

## 二、游戏引擎 / 状态机

### 10. 你为什么用状态机设计狼人杀流程？
**标准答案：**  
狼人杀本质上是一个分阶段、强约束的回合制游戏。状态机能把 `WAITING / NIGHT / DAY / VOTE / END` 这些阶段清晰地拆开，每个阶段只允许对应的操作，这样规则更清楚，代码更容易维护，也方便以后扩展新角色或新流程。

### 11. `WAITING / NIGHT / DAY / VOTE / END` 状态之间是怎么流转的？
**标准答案：**  
`WAITING` 是准备阶段，玩家准备完成后进入 `NIGHT`。夜晚阶段依次处理守卫、狼人、预言家、女巫等行动，结算后进入 `DAY`。白天阶段先发言，再进入 `VOTE` 投票。投票结束后判断是否有人出局，再检查胜负条件，若未结束则回到下一个夜晚，否则进入 `END`。

### 12. 状态机的数据结构是怎么设计的？
**标准答案：**  
状态机通常会维护当前阶段、存活玩家、角色分配、夜间行动记录、投票记录、发言顺序和历史事件。阶段切换由统一的推进函数控制，而不是分散在多个接口里。这样可以避免状态错乱，也方便做回放和复盘。

### 13. 为什么要把游戏状态和业务接口分开？
**标准答案：**  
因为接口只负责接收请求和返回结果，而游戏规则判断应该集中在引擎里。把状态校验、技能结算、胜负判断都放在引擎内部，可以避免规则散落在各个路由和 Socket 事件里，后续改规则也更安全。

### 14. 狼人、预言家、女巫、猎人、守卫这些角色的行动顺序怎么控制？
**标准答案：**  
我会把夜晚行动拆成固定顺序，先守卫，再狼人，再预言家，再女巫，最后统一结算。这样可以明确先后关系，避免多个角色同时改状态导致冲突。所有夜间行为先记录，等阶段结束后统一计算结果。

### 15. 女巫救人和毒人如何处理冲突？
**标准答案：**  
女巫的救人和毒人属于同一阶段内的不同决策，应该先记录动作，再在结算阶段统一处理。结算时按照规则判断：被守卫保护、被救、被毒等情况分别计算，最终只生成一个一致的夜晚结果，避免中途多次修改状态。

### 16. 猎人死亡后开枪是同步处理还是异步处理？
**标准答案：**  
猎人开枪通常是在死亡事件触发后进入一个短暂的决策流程，再由引擎统一结算。这样可以保证猎人的技能是“死亡触发”的，而不是和其他阶段混在一起。若超时未选择，则按规则自动处理。

### 17. 投票平票怎么处理？
**标准答案：**  
平票要按项目规则提前定义清楚。常见做法是平票则无人出局，或者进入下一轮发言后重新投票。关键是投票阶段结束时由统一函数结算，而不是依赖前端判断，避免客户端状态不一致。

### 18. 胜负判定逻辑是怎么写的？
**标准答案：**  
胜负判定核心就是检查阵营存活人数。一般来说，狼人全部出局则好人胜利；当狼人数量达到或超过好人数量时，狼人胜利。这个判断应该在每次关键结算后统一执行，保证游戏能够及时结束。

### 19. 如果以后新增角色，比如白痴、骑士、丘比特，你的状态机怎么扩展？
**标准答案：**  
我会把角色能力拆成独立的行动处理器，并把阶段流程和角色逻辑解耦。新增角色时，只需要在对应阶段插入动作处理和结算规则，不需要改整个游戏主流程。这样扩展成本会低很多。

### 20. 如何防止玩家在非自己回合发送非法操作？
**标准答案：**  
后端会做二次校验，判断当前阶段、玩家身份、存活状态、是否轮到该玩家以及目标是否合法。前端可以隐藏按钮，但真正的权限判断必须在服务端完成，因为前端不能作为可信来源。

### 21. 游戏状态是存在内存还是数据库？
**标准答案：**  
对局进行中的实时状态放在内存里，因为频繁读写，内存响应最快。数据库主要保存用户、房间、聊天记录、操作日志和最终游戏结果。这样兼顾实时性和持久化需求。

### 22. 如果服务端重启，正在进行的游戏能否恢复？
**标准答案：**  
如果只依赖内存，重启后实时对局会丢失，所以需要额外的快照或事件日志机制。当前项目重点是实时对局逻辑，后续如果要增强恢复能力，可以把游戏快照定期持久化到 Redis 或 MySQL，重启后再按快照恢复。

## 三、Socket.IO / 实时通信

### 23. 你用了哪些 Socket.IO 事件？
**标准答案：**  
主要包括创建房间、加入房间、离开房间、准备状态、开始游戏、夜间行动、投票、发言、猎人开枪、阶段切换、游戏结束和断线重连等事件。公共信息会广播到房间，私密信息只发给对应玩家。

### 24. 客户端和服务端怎么保持游戏状态一致？
**标准答案：**  
原则是“服务端为准”。客户端收到事件后只负责渲染界面和更新展示状态，真正的状态变化由服务端统一计算并广播。这样即使有延迟、断线或多人同时操作，也能保证状态一致。

### 25. 玩家断线重连后如何恢复状态？
**标准答案：**  
我会通过 `userId -> socketId` 的映射重新绑定连接，并从服务端当前房间状态里恢复角色、存活状态、阶段信息和可操作权限。重连后客户端再重新订阅房间事件，就能继续当前对局。

### 26. 如果一个用户多设备登录，你怎么处理？
**标准答案：**  
同一个 `userId` 只保留一个有效连接，新连接建立后会踢掉旧连接，或者标记旧连接失效。这样可以避免同一用户从多个设备同时操作同一个角色，保证游戏公平性。

### 27. `userId -> socketId` 映射存在哪里？
**标准答案：**  
通常会放在后端内存或缓存工具里，便于快速查找和推送消息。它的作用是把“用户身份”和“当前连接”关联起来，方便单播、断线恢复和多设备控制。

### 28. Socket 连接断开后，房间里的玩家状态怎么更新？
**标准答案：**  
断开时不会直接把玩家删掉，而是先把状态标记为离线或断线保留。如果在允许的重连窗口内重新连上，就恢复连接；如果超时未恢复，再按规则移除或判定为退出。

### 29. 如何保证私密信息只发给对应玩家？
**标准答案：**  
所有私密信息都由后端单独推送给对应 `socketId`，不会广播到整个房间。比如预言家查验结果、狼人队友信息、女巫药水状态，都是严格按身份过滤后发送。

### 30. 如果多个 Socket 事件几乎同时到达，如何避免状态错乱？
**标准答案：**  
后端会把阶段处理做成串行逻辑，按当前状态依次处理请求，并在处理前做合法性检查。也就是说，状态修改不能由多个事件同时直接写入，而要经过统一的引擎入口。

### 31. 服务端重启后 Socket 状态丢失怎么办？
**标准答案：**  
Socket 本身是短连接状态，重启后一定会丢失，所以要靠用户重新连接和房间状态恢复。真实业务里通常会把关键状态外置到 Redis 或数据库，Socket 只负责传输，不负责长期存储。

## 四、AI Agent / LangChain

### 32. AI 玩家是如何接入游戏的？
**标准答案：**  
AI 玩家在系统里和真人玩家统一成玩家实体，但它的行动来源是后端 Agent。每当轮到 AI 执行动作时，后端会把当前阶段、角色、历史信息和人格参数发给 AI，再把 AI 的输出结果转换成游戏引擎能识别的操作。

### 33. AI 和真人玩家的数据结构一样吗？
**标准答案：**  
核心字段基本一致，比如角色、存活状态、座位号、所属房间等都一样。区别主要在于是否为 AI、是否有 socket 连接、以及行动是否来自模型输出。这样统一建模后，游戏引擎处理起来会更简单。

### 34. AI 的 Prompt 是怎么设计的？
**标准答案：**  
Prompt 里会包含 AI 的身份、阵营、当前阶段、存活玩家、历史发言、投票记录、夜间行动结果和人格参数。核心目标是让 AI 只基于它应该知道的信息做决策，避免信息泄露和行为失控。

### 35. 你如何让 AI 知道自己的角色？
**标准答案：**  
我会在上下文里明确告诉它当前身份、阵营和可用能力，同时附加阶段约束。比如狼人只知道队友和可刀目标，预言家只能查验一名玩家，女巫只知道是否有人死亡及自己的药水状态，不能越权读取全局信息。

### 36. 如何防止 AI 泄露不该知道的信息？
**标准答案：**  
关键是上下文隔离。不同角色拿到的 Prompt 内容不同，后端在组装上下文时就已经过滤掉了不该知道的信息。然后在输出阶段再做一次校验，避免 AI 说出身份泄露或非法操作。

### 37. AI 发言时上下文包含哪些内容？
**标准答案：**  
通常包含当前阶段、历史发言摘要、上一轮投票结果、已死亡玩家、已知公开信息、角色身份和人格参数。这样 AI 的发言不会是孤立的句子，而是和当前局势有联系的。

### 38. AI 投票逻辑是完全交给大模型，还是有规则约束？
**标准答案：**  
不是完全交给模型。模型负责给出倾向和理由，真正的合法性判断由后端完成。后端会检查目标是否存活、是否允许投票、是否符合当前阶段，以及是否需要 fallback，确保结果稳定。

### 39. AI 夜间行动是如何决策的？
**标准答案：**  
夜间决策时，后端会把角色信息、局势、候选目标和人格特征发给模型，模型输出一个结构化结果，比如目标玩家和理由。然后后端再校验这个目标是否合法，不合法就用预设策略兜底。

### 40. 如果 AI 返回格式不稳定怎么办？
**标准答案：**  
我会尽量要求 AI 输出结构化 JSON，并在后端做解析和校验。如果解析失败，就启动 fallback 逻辑，比如重新请求、使用模板策略或者按照规则自动选择，确保游戏流程不中断。

### 41. 你有没有做 JSON 格式约束或输出解析？
**标准答案：**  
有。AI 输出最好统一成结构化格式，这样后端更容易解析、验证和落地。相比纯自然语言，JSON 能明显降低歧义，特别适合投票、夜间行动这种需要精确执行的场景。

### 42. 如何控制 AI 响应时间？
**标准答案：**  
我会限制上下文长度、控制历史信息摘要化，并设置超时机制。对局过程不能因为 AI 卡住而阻塞，所以 AI 请求必须有时间上限，超时后直接走 fallback。

### 43. 如何控制 token 成本？
**标准答案：**  
主要靠三点：减少无关历史、对长对话做摘要、不同任务用不同长度的 Prompt。比如发言需要更多上下文，但夜间行动只需要关键状态，不必把整局完整聊天都塞进去。

### 44. 如果 AI 请求失败，游戏流程怎么继续？
**标准答案：**  
不能让游戏卡死，所以要有降级方案。AI 请求失败后，系统可以使用默认策略、随机策略或超时默认动作继续流程，保证对局不中断。

### 45. AI 是否会读取全部游戏历史？历史太长怎么办？
**标准答案：**  
不会直接把全部原始历史塞进去。更合理的做法是只保留最近几轮的关键发言和局势摘要，旧信息做压缩总结。这样既保留推理线索，也能控制上下文长度。

### 46. LangChain 在项目中具体起什么作用？
**标准答案：**  
LangChain 主要负责把 Prompt 组织、模型调用、输出解析和链式处理串起来。它不是简单“调一次模型”，而是帮助我把 AI 决策流程工程化，方便后续扩展不同角色和不同策略。

## 五、人格系统

### 47. AI 人格参数有哪些？
**标准答案：**  
项目里的人格参数通常包括激进度、谨慎度、狡猾度、诚实度、话痨度等。它们分别影响 AI 的攻击性、风险偏好、伪装能力、撒谎倾向和发言长度。

### 48. 激进度、谨慎度、狡猾度、诚实度分别影响什么？
**标准答案：**  
激进度高的 AI 会更主动带票和怀疑别人；谨慎度高的 AI 会更保守，发言更留余地；狡猾度高的狼人更会伪装和误导；诚实度高的 AI 更少编造信息，更倾向于稳定输出。

### 49. 人格参数是规则控制还是 Prompt 控制？
**标准答案：**  
两者结合。人格参数会先转成策略描述写进 Prompt，同时后端也可以在某些行为上做规则约束，比如发言长度、目标选择范围、投票偏好等。这样既有自然语言表现，也有可控性。

### 50. 如何保证不同 AI 的表现真的不一样？
**标准答案：**  
我会通过不同的人格参数、不同的随机种子、不同的发言策略和不同的上下文权重来拉开行为差异。这样同样是 AI 玩家，也会表现出不同的攻击性、风格和推理偏好。

### 51. 如果两个 AI 人格相同，会不会表现完全一样？
**标准答案：**  
不会完全一样。因为局势、历史上下文和随机性都会影响决策。不过如果参数和上下文都完全一致，行为会更接近，所以通常会给不同 AI 配置略有差异的人格，避免表现太机械。

### 52. AI 狼人如何伪装成好人？
**标准答案：**  
狼人通常会根据局势选择合理站边、解释自己的投票逻辑，并避免过早暴露攻击性。人格系统里的狡猾度和诚实度会影响它是偏“强势带节奏”还是偏“低调埋伏”。

### 53. AI 好人如何根据历史发言推理？
**标准答案：**  
好人 AI 会把历史发言、投票轨迹和夜晚结果结合起来，寻找矛盾点和可疑目标。它不是只看单句，而是看行为模式，比如谁的发言和投票前后不一致，谁在关键轮次回避表态。

## 六、数据库 / 持久化 / 前端 / 部署

### 54. MySQL 表是怎么设计的？
**标准答案：**  
通常会分成用户表、房间表、对局记录表、玩家对局记录表、聊天记录表和行动日志表。这样既能存用户基础信息，也能保留一局游戏从开始到结束的完整痕迹，便于查询和复盘。

### 55. 一局游戏的过程如何存储？
**标准答案：**  
实时过程主要在内存中流转，关键节点会落库，比如玩家身份、关键技能使用、投票结果、死亡结果和最终胜负。这样既不会拖慢对局，也能在结束后回看整局过程。

### 56. 聊天记录和游戏行动记录分开存吗？
**标准答案：**  
建议分开存。聊天记录偏展示和复盘，行动记录偏规则和状态变化。分开之后，查询更清晰，也方便以后按不同维度做统计和回放。

### 57. 如何支持游戏复盘？
**标准答案：**  
复盘的关键是保存结构化事件。比如发言、投票、死亡、查验、救人、开枪这些节点都记录下来，结束后就可以按时间线回放，帮助玩家分析每一步为什么会输赢。

### 58. 前端 Pinia 中存了哪些状态？
**标准答案：**  
一般会存用户登录状态、房间信息、游戏阶段、当前玩家列表、自己角色、聊天记录、投票状态和操作权限。这样页面可以根据阶段自动切换视图，不需要到处手动传参。

### 59. Socket 事件在哪里监听？
**标准答案：**  
通常会在前端统一封装一个 socket 模块或 composable，再由 Pinia 或页面组件订阅事件。这样可以把连接、断开、重连和事件分发集中管理，避免逻辑散落在多个组件里。

### 60. Vue3 Composition API 你是怎么用的？
**标准答案：**  
主要用来拆分状态和逻辑，比如把房间状态、游戏状态、Socket 监听、计时器和确认弹窗封装成独立逻辑块。这样页面组件会更薄，复用和维护都更方便。

### 61. 前端只负责展示吗？
**标准答案：**  
前端不仅负责展示，也负责基本交互控制，比如根据阶段显示按钮、禁用非法操作、展示倒计时和玩家列表。但前端不能决定规则，真正的合法性校验一定在后端完成。

### 62. Docker 在项目里怎么用的？
**标准答案：**  
Docker 主要用来把前端、后端和数据库环境标准化，方便本地开发和部署。通过 `Dockerfile` 和 `docker-compose` 可以一键启动整套环境，避免“我本地能跑，你那边不能跑”的问题。

### 63. 本地开发和生产部署有什么区别？
**标准答案：**  
本地开发更关注热更新和调试，通常前后端分开启动；生产部署更关注稳定性和可运维性，会用容器、反向代理和环境变量管理。AI Key、数据库密码这类敏感配置也应该只放在环境变量里。

### 64. 这个项目最能体现你能力的点是什么？
**标准答案：**  
最能体现能力的是把复杂的狼人杀规则、实时同步和 AI 决策统一到一套可维护的系统里。它不是单点功能堆砌，而是状态机、Socket、Prompt 工程和数据持久化的组合设计。

## 七、面试临场补充模板

### 65. 如果面试官让你一句话总结项目，你怎么说？
**标准答案：**  
这是一个基于 Vue3、Node.js、Socket.IO 和 LangChain 的多人在线狼人杀平台，支持真人和 AI 混合对局，重点解决了实时状态同步、游戏状态机和 AI 智能体决策三个问题。

### 66. 如果面试官追问“你做了什么”，你怎么答？
**标准答案：**  
我主要负责游戏引擎和 AI 部分，前者保证狼人杀规则正确执行，后者让 AI 能在不同阶段根据局势做出发言、投票和夜间行动。与此同时，我还处理了断线重连、多设备登录控制和对局数据持久化。

### 67. 如果面试官问“你最大的收获是什么”，你怎么答？
**标准答案：**  
最大的收获是理解了复杂业务不能靠零散接口拼起来，而要先建模、再抽象、再落地。状态机让规则清晰，事件驱动让同步可靠，结构化 Prompt 让 AI 可控，这三点是这个项目最核心的工程经验。

